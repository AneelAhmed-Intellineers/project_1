""" if request.method == 'POST':
        data = Student.objects.all()
        flag = True
        for student in data:
            if student.enrollment == request.POST.get('enrollment_field'):
                result = student.grade
                flag = False
                return HttpResponse("Your result for Enrollment number is : %s" % request.POST.get('enrollment_field') %result)
        if flag is False:

            return ("Your result for enrollment number %s is not yet updated, Please recheck again after some time." %request.POST.get('enrollment_field'))
    """
 



 grade = Grade("")
    if request.method == 'POST':
            data = Student()
            data.name = request.POST.get('name_field')
            data.email = request.POST.get('email_field')
            data.enrollment = request.POST.get('enrollment_field')
            data.grade = grade
            data.save()
            form = StudentForm()
            return render(request, '/home/aneel/Development/project_1/project_1/app/templates/forms.html', {'form':form})

    else:
        form = StudentForm()
        return  render(request, '/home/aneel/Development/project_1/project_1/app/templates/forms.html',{'form' : form})